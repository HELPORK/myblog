<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('./partials/head') %>
    <title>search Home Page</title>
</head>
<body>
      <%- include('./partials/nav') %>   

      <div class="container">
      <div class=" row row-cols-3 ">
          <% blogs.forEach(blog => { %>
     <div class="col mt-5">
          <div class="card" style="width: 18rem;">
  <img src="<%= blog.coverImageURL %>" class="card-img-top" width="100px" height="220px" alt="...">
  <div class="card-body">
    <h5 class="card-title"><%= blog.title %></h5>
    <a href="/blog/<%= blog._id %>" class="btn btn-primary">View</a>
  </div>

     </div>
</div>
        <% }) %>  </div>
</div>

   

      <div class="container">
      <div class=" row row-cols-3 ">
        <% userBlogs.forEach(blog => { %>
     <div class="col mt-5">
          <div class="card" style="width: 18rem;">
  <img src="<%= blog.coverImageURL %>" class="card-img-top" width="100px" height="220px" alt="...">
  <div class="card-body">
    <h5 class="card-title"><%= blog.title %></h5>
    <a href="/blog/<%= blog._id %>" class="btn btn-primary">View</a>
  </div>

     </div>
</div>
        <% }) %>  </div>
</div>
      <%- include('./partials/script') %>  
</body>
</html>